{
  "packages": ["packages/*"],
  "version": "independent",
  "command": {
    "publish": {
      "ignoreChanges": ["**/*.md"]
    },
    "version": {
      "message": "chore(release): publish version",
      "ignoreChanges": ["**/*.md"]
    }
  }
}
